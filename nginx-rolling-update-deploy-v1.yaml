apiVersion: apps/v1
kind: Deployment
metadata:
  name: tomcat-server
  labels:
    app: tomcat
  annotations:
    kubernetes.io/change-cause: version 9.0 -> 8.0
  namespace: test
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: tomcat
  template:
    metadata:
      name: tomcat-server
      labels:
        app: tomcat
    spec:
      containers:
        - name: tomcat-server
          image: tomcat:9.0
          ports:
            - containerPort: 8080
